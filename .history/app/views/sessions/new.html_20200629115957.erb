<%= form_for :user, url: '/signin' do |f| %>
  <p>User: <%= f.collection_select :name, User.all, :id, :name %></p><br/>
  Password: <%= f.text_field :password %>
  <%= f.submit "Submit" %>
<% end %>